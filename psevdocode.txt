const points_num = 197768

class path {
    вектор из точек
    длина
}

best_way = path([...], 1.0 / 0.0)

while (true):
    const ant_cum = 10;
    new first_points = (random point, ... * ant_num)
    new paths = path[ant_num]
    for each i : first_points:
        пройденные_точки = [...];
        пройденные_точки[0] = i
        НЕпройденные_точки = [0, 1, ... 197768] - i

        while (НЕпройденные_точки.size != 0):
            идти в следующую рандомно (расстояние + ферамон)
            (
                1) посмотреть из даты все расстояния и ферамон
                2) по формуле расчитать вероятности пройти в каждую точку
                3) выбрать через столбик
            )

            точка пройдена
            (
                пройденные_точки[точка] = true
            )
        посчитать длину путь при помощи пройденнх точек
        (
            len = 0
            for i to 197768 - 1
                len += data[пройденные_точки[i]]
                            [пройденные_точки[i+1]].first
        )

        tmp_way = path(пройденные_точки, длина только что пройденного пути)

        paths добавить tmp_way

        if tmp_way.size < best_way.size
            best_way = tmp_way
            print(best_way)

    обновляем ферамон
    (
        Q = 4
        L = длина пути
        0 < p < 1

        for each paths:
            умножаем феромон в каждом ребре на p
        for each paths:
            в каждом ребре прибавляем ферамон по форуле Q/L
    )
